# =============================================================================
# 自媒体创作助手 - 环境变量模板
# =============================================================================
# 复制此文件为 .env 或 ~/.env.creator 并填入真实值
# cp .env.example ~/.env.creator && vim ~/.env.creator
# =============================================================================

# ---- 必填 ----

# 阿里云 DashScope API Key（通义千问）
# 获取方式: https://dashscope.console.aliyun.com/
DASHSCOPE_API_KEY=sk-your-api-key-here

# ---- Telegram（推荐渠道）----

# Telegram Bot Token（从 @BotFather 获取）
TELEGRAM_BOT_TOKEN=your-bot-token-here

# 你的 Telegram 数字 User ID（从 @userinfobot 获取）
TELEGRAM_USER_ID=your-user-id-here

# ---- 可选 ----

# YouTube Data API Key（用于数据拉取，非必须，可走 Browser RPA 替代）
# 获取方式: https://console.cloud.google.com/apis/credentials
YOUTUBE_API_KEY=

# OpenClaw 数据目录（默认 ~/.openclaw）
OPENCLAW_HOME=$HOME/.openclaw

# 时区（默认 Asia/Shanghai）
TZ=Asia/Shanghai

# VNC 密码（用于远程登录浏览器，至少 6 位）
VNC_PASSWORD=

# ---- 以下由系统自动管理，无需手动填写 ----
# OPENCLAW_PORT=3000
